#include <iostream>
using namespace std;

int main()
{
    int *arr, n, i;

    cout << "Enter number of elements: ";
    cin >> n;

    // Dynamic Memory Allocation
    arr = new int[n];

    cout << "Enter elements: ";
    for(i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    cout << "Array elements are: ";
    for(i = 0; i < n; i++)
    {
        cout << arr[i] << " ";
    }

    // Free allocated memory
    delete[] arr;

    return 0;
}
